
{%
  set classes = [
    'header',
    'region',
    'region-' ~ region|clean_class,
  ]
%}
{% if content %}
  <header{{ attributes.addClass(classes) }}>
      <div class="header_main">
          {{ content }}
      </div>
  </header>
{% endif %}
